---
import MainLayout from '../layouts/MainLayout.astro';
import { Image } from 'astro:assets';
---
<MainLayout>
  <div class="bg-black min-h-screen">
    <section class="max-w-5xl mx-auto pt-40 px-8 md:px-16 space-y-32 text-white">
      <div class="border-t border-black pt-10 grid grid-cols-1 md:grid-cols gap-10 scroll-animate">
        <!-- Tekstkolonne -->
        <div class="flex flex-col justify-between h-full max-w-prose mx-auto text-left">
          <h2 class="text-2xl mb-4">Vincent Groos - Direktør & CFO</h2>
          <p class="text-sm leading-relaxed mt-auto">
            Vincent har en baggrund i europæisk business og strategisk ledelse og står for forretningsudvikling og økonomi i Balash. Han brænder for brands, der bygger relationer og for mad som oplevelse. Hans styrke er at tænke i helheder – både for gæsten og for virksomheden.
          </p>
        </div>

        <!-- Tekstkolonne -->
        <div class="flex flex-col justify-between h-full max-w-prose mx-auto text-left">
          <h2 class="text-2xl mb-4">Emil Dos Santos Andersen - Køkkenchef & Kreatør</h2>
          <p class="text-sm leading-relaxed mt-auto">
            Emil er Balashs autodidakte kok og kreative kraft. Han har arbejdet på nogle af Københavns mest kompromisløse restauranter, og i dag leder han køkkenet med kompromisløshed og fantasi. Emil bygger retter op som fortællinger – og han ved præcis, hvordan mad skal smage, føles og se ud.
          </p>
        </div>

        <!-- Tekstkolonne -->
        <div class="flex flex-col justify-between h-full max-w-prose mx-auto text-left">
          <h2 class="text-2xl mb-4">Bertram Blauenfeldt Otkjær - Vinkoordinator & Eventleder</h2>
          <p class="text-sm leading-relaxed mt-auto">
            Bertram er vores vinmand og detaljeorienterede tovholder. Han har serveret vin og skabt stemning i fem år som bestyrer på Pio Vinbar. I Balash binder han det hele sammen mellem køkken, klient og koncept – med en sikker hånd og altid med sans for, hvad gæsten har brug for, før de selv ved det.
          </p>
        </div>
      </div>
    </section>
  </div>

  <style>
    .scroll-animate {
      opacity: 0;
      transform: translateY(40px);
      transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }

    .scroll-animate.show {
      opacity: 1;
      transform: translateY(0);
    }
  </style>

  <script client:load>
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('show');
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.scroll-animate').forEach(el => observer.observe(el));
  </script>
</MainLayout>
